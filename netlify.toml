[build]
  publish = "public"
  command = "cd themes/benmerde && npm install && grunt production && cd ../.. && hugo"

[context.production.environment]
  HUGO_VERSION = "0.37"
  HUGO_ENV = "netlify"

[context.deploy-preview.environment]
  HUGO_VERSION = "0.37"

[context.branch-deploy.environment]
  HUGO_VERSION = "0.37"